# Changelog

## [0.1.0] - 2026-07-13
### Added
- Reimplemented IMUSim as a pure Python package with `numpy` and optional `torch` backends.
- Added `pyproject.toml`, `setup.cfg`, and `MANIFEST.in` for package metadata.
- Added `examples/demo_simple.py` and `examples/demo_torch_gpu.py`.
- Added test coverage for simulation, math, and trajectory modules.
- Added install and usage documentation in `README.md`.
- Added `requirements.txt`.
